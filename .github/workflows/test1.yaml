name: test1
on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/test1.yaml'

jobs:
  job1:
    runs-on: ubuntu-latest
  
    steps:
      - name: ssh
        run: ssh -T git@github.com